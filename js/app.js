/**
 * Main app file. Initializes app components.
 * 
 * The main app object.
 *
 */

const vanillaPress = {

    init: function() {

        model.init();
        view.init();
        router.init();

    }

};
vanillaPress.init();
